#include <stdio.h>
#include <stdlib.h>

char *huffmanCoding(char *str)
{
    char *result;

    int n = sizeof(str) / sizeof(char);

    int count_diff_char = 0;

    for (int i = 0; i < n; i++)
    {
    }

    return result;
}